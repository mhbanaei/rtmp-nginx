
#user  nobody;
# multiple workers works !
worker_processes  1;

#error_log  logs/error.log;
#error_log  logs/error.log  notice;
#error_log  logs/error.log  info;

#pid        logs/nginx.pid;


events {
    worker_connections  1024;
    # max value 32768, nginx recycling connections+registry optimization = 
    #   this.value * 20 = max concurrent connections currently tested with one worker
    #   C1000K should be possible depending there is enough ram/cpu power
    # multi_accept on;
}


			#exec_push C:/ffmpeg/bin/ffmpeg.exe -re -i rtmp://127.0.0.1/live/$name -c:v libx264 -preset veryfast -b:v 2500k -maxrate 2500k -bufsize 5000k -c:a aac -b:a 128k -f flv "rtmp://dus01.contribute.live-video.net/app/live_103730834_oDulJIscNiy6831O92eHBVgk93AaES";


rtmp {
    server {
        listen 1935;  
        chunk_size 4096;

        application live1 {
            live on;
            record off;
            hls off;
            dash off;
            wait_key on;
			
            on_publish http://127.0.0.1:8000/auth?server=live1;
            push rtmp://a.rtmp.youtube.com/live2/Stream_Key;
            push rtmp://dus01.contribute.live-video.net/app/Stream_Key;
            #exec_publish "C:/ffmpeg/bin/ffmpeg.exe" -i rtmp://127.0.0.1/live1/test -async 1 -vsync -1 -c copy -f flv "rtmp://rtmp.cdn.asset.aparat.com:443/event/Stream_Key";
			#Morede Balaei chon aparat mored dare az system ffmpeg estefade mishe 
			#https://www.gyan.dev/ffmpeg/builds/
			#ffmpeg-release-essentials.zip | "in bayad download beshe"
			#to properties my pc > Advanced system settings
			#Environment Variables
			#System variables > Path  > C:\ffmpeg\bin
			# baraye check kardan > ffmpeg -version
        }
    }
}

